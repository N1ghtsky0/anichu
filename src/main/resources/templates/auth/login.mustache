<!DOCTYPE html>
<html lang="ko">
<head>
  <title>애니추</title>
  {{>common/header}}
  <style>
    .hr-sect {
      display: flex;
      flex-basis: 100%;
      align-items: center;
      color: rgba(0, 0, 0, 0.35);
      font-size: 16px;
      margin: 8px 0;
    }
    .hr-sect::before,
    .hr-sect::after {
      content: "";
      flex-grow: 1;
      background: rgba(0, 0, 0, 0.35);
      height: 1px;
      font-size: 0;
      line-height: 0;
      margin: 0 16px;
    }
    .join-icon {
      max-height: 40px;
      max-width: 175px;
    }
  </style>
</head>
<body style="background-color: lightgrey">
{{>common/navbar}}
<div class="container p-3">
  <div class="p-3" style="background-color: white; border-radius: 20px">
    <form method="post">
      <input type="hidden" name="{{_csrf.parameterName}}" value="{{_csrf.token}}">
      <h2 class="mb-2">로그인</h2>
      <hr class="mb-4">
      <div class="mb-3">
        <label for="inputEmail" class="form-label">이메일<span style="color: red"> *</span></label>
        <input type="email" class="form-control" id="inputEmail" name="email" {{#userVO}}value="{{userVO.email}}"{{/userVO}} />
      </div>
      <div class="mb-3">
        <label for="inputPassword" class="form-label">비밀번호<span style="color: red"> *</span></label>
        <input type="password" class="form-control" id="inputPassword" name="password">
      </div>
      <button type="submit" class="btn btn-primary">로그인</button>
    </form>
    <div class="hr-sect mb-4">또는</div>
    <div class="row d-flex justify-content-evenly">
      <div class="col-md-10 col-lg-4 mb-3 text-center">
        <a href="#!">
          <img src="/images/google_login.svg" class="join-icon order-0" alt="google sign in">
        </a>
      </div>
      <div class="col-md-10 col-lg-4 mb-3 text-center">
        <a href="#!">
          <img src="/images/naver_login.png" class="join-icon order-1" alt="naver sign in">
        </a>
      </div>
      <div class="col-md-10 col-lg-4 mb-3 text-center">
        <a href="#!">
          <img src="/images/kakao_login.png" class="join-icon order-2" alt="kakao sign in">
        </a>
      </div>
    </div>
  </div>
</div>
{{>common/footer-script}}
</body>
</html>